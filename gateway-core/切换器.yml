
# 限流算法切换开关
limiter:
  type: redis      # 现在用Redis令牌桶（分布式版）
  # type: local   # 想切回单机版时，把上面注释掉，这个解开

# 其他配置...
spring:
  application:
    name: gateway-service
  redis:
    host: localhost
    port: 6379